import { initializeApp, cert } from 'firebase-admin/app'; import { getFirestore } from 'firebase-admin/firestore'; import { readFileSync } from 'fs'; const serviceAccount = JSON.parse(readFileSync('./centro-operativo-cpsl-65ad52160f45.json')); initializeApp({ credential: cert(serviceAccount) }); const db = getFirestore(); async function run() { const snap = await db.collection('user_profiles').get(); let profilesCount = 0; let studentsCount = 0; snap.forEach(doc => { const d = doc.data(); profilesCount++; if(d.role === 'student' || d.role === 'estudiante') studentsCount++; }); console.log('Profiles users:', profilesCount); console.log('Students in Profiles:', studentsCount); process.exit(0); } run();
