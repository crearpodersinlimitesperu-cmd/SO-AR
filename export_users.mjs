import { initializeApp, cert } from 'firebase-admin/app'; import { getFirestore } from 'firebase-admin/firestore'; import { readFileSync, writeFileSync } from 'fs'; const serviceAccount = JSON.parse(readFileSync('./centro-operativo-cpsl-65ad52160f45.json')); initializeApp({ credential: cert(serviceAccount) }); const db = getFirestore(); async function run() { const snap = await db.collection('users').get(); const users = []; snap.forEach(doc => { const d = doc.data(); users.push({ email: d.email || 'Sin correo', nombre: d.name || d.displayName || 'Sin nombre', rol: d.role || 'Sin rol definido', sede: d.sede || 'Global' }); }); writeFileSync('usuarios_exportados.json', JSON.stringify(users, null, 2)); console.log('Exported ' + users.length + ' users'); process.exit(0); } run();
