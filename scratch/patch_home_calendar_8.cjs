const fs = require("fs"); let code = fs.readFileSync("src/pages/Home.jsx", "utf8"); let start = code.indexOf("<button onClick={() => { setShowToolsDropdown(false); navigate('/calendario-equipo'); }}"); console.log(start); let end = code.indexOf("</button>", start) + 9; let oldBtn = code.substring(start, end); console.log("oldBtn", oldBtn); code = code.replace(oldBtn, `{(currentUser?.isSuperAdmin || currentUser?.appRole === 'gerente' || currentUser?.isDireccion || currentUser?.appRole === 'director_maestria') && (\n                ${oldBtn}\n              )}`); fs.writeFileSync("src/pages/Home.jsx", code, "utf8"); console.log("Done");
