const fs = require("fs"); let code = fs.readFileSync("src/pages/Home.jsx", "utf8"); let startIndex = code.indexOf("<button onClick={() => { setShowToolsDropdown(false); navigate('/calendario-equipo'); }}"); code = code.replace("<button onClick={() => { setShowToolsDropdown(false); navigate('/calendario-equipo'); }} className=\"btn-secondary\" style={{ textAlign: 'left', padding: '0.5rem', fontSize: '0.82rem', justifyContent: 'flex-start', color: '#f97316', background: 'rgba(249, 115, 22, 0.1)' }}>\n                  🗓️ Agenda y Time Boxing\n                </button>", "{(currentUser?.isSuperAdmin || currentUser?.appRole === 'gerente' || currentUser?.isDireccion || currentUser?.appRole === 'director_maestria') && (\n                  <button onClick={() => { setShowToolsDropdown(false); navigate('/calendario-equipo'); }} className=\"btn-secondary\" style={{ textAlign: 'left', padding: '0.5rem', fontSize: '0.82rem', justifyContent: 'flex-start', color: '#f97316', background: 'rgba(249, 115, 22, 0.1)' }}>\n                    🗓️ Agenda y Time Boxing\n                  </button>\n                )}"); fs.writeFileSync("src/pages/Home.jsx", code, "utf8"); console.log("Done");
