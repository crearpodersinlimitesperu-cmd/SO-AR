const fs = require('fs'); let code = fs.readFileSync('src/pages/Home.jsx', 'utf8'); code = code.replace(/<button onClick=\{\\(\\) => navigate\\('\\/generador-flyer'\\)\\}[\\s\\S]*?<\\/button>/g, match => {(currentUser?.isSuperAdmin || currentUser?.appRole === 'gerente' || currentUser?.isDireccion || currentUser?.appRole === 'director_maestria' || currentUser?.appRole?.includes('coord')) && (\n\n)}); fs.writeFileSync('src/pages/Home.jsx', code, 'utf8');
